{
  "private": true,
  "workspaces": [
    "packages/*",
    "sandbox"
  ],
  "name": "ds-pack-components",
  "scripts": {
    "build": "yarn workspace @ds-pack/components run build",
    "test": "yarn workspace @ds-pack/components run test",
    "pub": "yarn workspace @ds-pack/components run pub",
    "start": "yarn workspace sandbox dev"
  },
  "dependencies": {}
}
