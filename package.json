{
  "private": true,
  "workspaces": [
    "packages/*",
    "sandbox"
  ],
  "name": "ds-pack-components",
  "scripts": {
    "build": "pnpm --filter @ds-pack/components build",
    "test": "pnpm --filter @ds-pack/components test",
    "pub": "pnpm --filter @ds-pack/components pub",
    "start": "pnpm --filter sandbox dev"
  },
  "dependencies": {}
}
